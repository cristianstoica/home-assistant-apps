# pyright: strict
"""py-syslog — a stdlib-only UDP syslog collector packaged as an HA add-on.

The importable package is the de-hyphenated form of the add-on slug
(``py-syslog`` cannot be a Python module name). Run with
``python3 -m pysyslog``.
"""

from __future__ import annotations

__version__ = "1.0.0"
